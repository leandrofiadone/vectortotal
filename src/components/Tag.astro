---
interface Props {
  name: string;
}

const { name } = Astro.props;
const tagClass = `tag-${name.toLowerCase()}`;
const href = `/tags/${name.toLowerCase()}`;
---

<a href={href} class:list={['tag', tagClass]}>{name}</a>

<style>
  .tag { text-decoration: none; }
</style>
